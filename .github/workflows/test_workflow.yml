name: TestWorkflow

on: workflow_call

permissions:
  contents: read
  packages: read

jobs:
  test-workflow:
    runs-on: [self-hosted]
    strategy:
      matrix:
        os: [ubuntu-24.04, ubuntu-22.04, ubuntu-20.04]

    steps:
    - name: workflow call test
      run: echo "TEST TEST TEST test"
